/*
 * create key (counting) array
 * for each element in list
 *     increase the respective counter by 1
 * for each counter, starting form smallest key
 *     while counter is non-zero
 *         restore element to list
 *         decrease counter by 1
 */
