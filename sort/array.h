#ifndef __UTILS_ARRAY_H_
#define __UTILS_ARRAY_H_

#include <stdio.h>
#include <stdlib.h>

#define TRUE 1
#define FALSE 0

int *rlist(int start, int end, int num);
void show(int *a, int len);

#endif

